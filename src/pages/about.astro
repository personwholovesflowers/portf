---
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import MyProjectCard from '@/components/MyProjectCard.astro'
import Layout from '@/layouts/Layout.astro'
import { getAllMyProjects } from '@/lib/data-utils'

const myprojects = await getAllMyProjects()
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="About" />
  <Breadcrumbs items={[{ label: 'About', icon: 'lucide:info' }]} />

  <section>
    <div class="min-w-full">
      <div class="prose mb-8">
        <p class="mt-0">
          13 yo front chatgpt coder from ukraine

        </p>
      </div>

      <!-- i dont have projects, so i can comment this ^_^ -->
      <!--<h2 class="mb-4 text-2xl font-medium">my project</h2>
      <div class="flex flex-col gap-4">
        {myprojects.map((myproject) => <MyProjectCard myproject={myproject} />)}
      </div>
       -->
    </div>
  </section>
</Layout>
